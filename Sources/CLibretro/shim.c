/* Header-only libretro API module.
 * The retro_* symbols are DECLARED by libretro.h and DEFINED by the statically
 * linked core (fceumm.xcframework); this translation unit exists only to satisfy
 * SwiftPM's requirement that a C target have at least one source file. */
#include "libretro.h"
